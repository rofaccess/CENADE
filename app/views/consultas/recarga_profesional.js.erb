$('.profesional_select').html("<%= escape_javascript(options_for_select(Hash[Empleado.where(type: 'Doctor', area_id:@area.id).map{|a| [a.area.nombre + "-"+ a.persona.nombre + " "+ a.persona.apellido, a.id]}]).insert(0, 'Seleccione un Profesional')) %>");
$('.profesional_select').select2('destroy');
$('.profesional_select').select2({
    placeholder: "Seleccione un Profesional",
    language: "es",
    theme: "bootstrap"

    }).on('change', function () {
        $(this).valid();

    });