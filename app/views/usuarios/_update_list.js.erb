$("div.content").html('<%= j render partial: 'list', format: 'html'%>');

<% if !flash.empty? %>
	$("#flash_messages").html('<%= j render partial: 'layouts/messages', format: 'html'%>');
<% end %>
