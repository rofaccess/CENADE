$('#modal-lg').modal('hide');

$('#paciente-data').html("<%= j render partial: params[:root].to_s()+'/form_paciente' %>");

$('.select-paciente').html("<%= escape_javascript(options_for_select(Hash[Paciente.all.map{|a| [a.persona.nombre + " "+ a.persona.apellido, a.id]}], selected: @paciente.id).insert(0, 'Seleccione un paciente')) %>");


//- Se usa solo con el select buscador
/*
$('.select-paciente').html("<%= escape_javascript(options_for_select([[@paciente.persona_nombre+" "+@paciente.persona_apellido, @paciente.id]], selected: @paciente.id).insert(0, 'Seleccione un paciente')) %>");
$('.select-paciente').select2({
  data: [{ id: "<%=j @paciente.id.to_s %>", text: "<%=j @paciente.persona_nombre %> <%=j @paciente.persona_apellido%> " }]
});

pacientesUI.initBuscarPaciente('.select-paciente');
*/