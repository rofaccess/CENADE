$("#principal-content").html('<%= j render 'form', from_ficha: false, consulta: @consulta, paciente: @paciente, doctores: @doctores, area: @area, formats: 'html' %>');

$('.ficha-id').val(<%=@paciente.ficha_nutricional_pediatrica.blank? ? '' : @paciente.ficha_nutricional_pediatrica.id%>);

