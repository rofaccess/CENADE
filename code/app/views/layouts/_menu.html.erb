  <nav class="navbar navbar-custom navbar-fixed-top">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        
       <% img= Configuracion.first.empresa_logo %> 
       <% if !img.url.nil? %> 
          <%= image_tag(img, size: "50x70", class:'img-polaroid img-', class: "navbar-brand") %>
        <%else%>
           <div class="navbar-brand"> CENADE </div>
        <%end%>


      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div>
        <ul class="nav navbar-nav">        

          <li class="<%= current_menu([welcome_index_turnos_path, new_turno_path])%>">
            <%= link_to '<span class="fa fa-calendar" aria-hidden="true"></span>  Turnos'.html_safe, new_turno_path  %> 
            
          </li> 
         
          <li class="<%= current_menu([pacientes_path])%>">
            <%= link_to '<span class="fa fa-user" aria-hidden="true"></span>  Pacientes'.html_safe, pacientes_path %>
          </li>
          
          <li class="<%= current_menu([welcome_index_fichas_path, welcome_index_atencion_profesional_path])%>">
            <%= link_to '<span class="fa fa-list-alt" aria-hidden="true"></span>  Fichas y Consultas'.html_safe, welcome_index_fichas_path %>
          </li>
          
          <li class="<%= current_menu([welcome_index_personal_path, empleados_path])%>">
            <%= link_to '<span class="fa fa-users" aria-hidden="true"></span>  Personal'.html_safe, empleados_path  %>            
          </li>

          <li class="<%= current_menu([welcome_index_reportes_path])%>">
            <%= link_to '<span class="fa fa-bar-chart" aria-hidden="true"></span>  Reportes'.html_safe, welcome_index_reportes_path  %>            
          </li>
          
          <li class="<%= current_menu([welcome_index_configuracion_path, usuarios_path, edit_configuracion_path(Configuracion.first),configuracion_path(Configuracion.first), roles_path, edit_user_registration_path])%>">
            <%= link_to '<span class="fa fa-gear" aria-hidden="true"></span>  Configuración'.html_safe, edit_configuracion_path(Configuracion.first)  %> 
            
          </li>  

        </ul>

        <ul class="nav navbar-nav navbar-right">
          <li><a href="#">
            <span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span>
            </a>
          </li>             

          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
              <sec:username/> 
              <span class="caret"></span>
            </a>
            
            <ul class="dropdown-menu" role="menu">                
              <li><%= link_to "Editar Perfil", edit_user_registration_path %></li>
              <li><%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete%></li>
            </ul>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>