$("#principal-content").html('<%= j render 'form', control: @control, area: @contol.area, doctores: @doctores, paciente: @control.paciente, formats: 'html' %>');

$("#list").html('<%= j render 'list', controles: @controles, format: 'html'%>');

$("#list-paginate").html('<%= j paginate(@controles, remote: true)%>');

